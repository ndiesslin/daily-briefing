services:
  daily-briefing:
    build: .
    restart: always
    volumes:
      - .:/app
      - ./output:/output
      - /var/run/cups/cups.sock:/var/run/cups/cups.sock
    # Environment variables can also be passed here
